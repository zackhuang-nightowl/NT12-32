/**
 * @file cap_agent.c
 * @brief Agent/diagnostics — NVR implements LOCAL handlers or returns 501.
 *
 * Standalone-camera stubs (getLog/getReportServer/getEnvironment/agent_diagnosis
 * etc.) removed.
 */
#include "business/business.h"

void cap_agent_register(nop_router_t *router)
{
    (void)router;
}
