{
  "_note": "⑤ 录像运行期计划。盘上格式/加密/多盘等编译期特性见 components/recorder/rsdk_features.conf",

  "default": {
    "mode": "continuous+event",
    "_note": "continuous=常录, event=事件录, continuous+event=常录且事件段打标",
    "prerecord_s": 5,
    "postrecord_s": 10
  },

  "event_types": ["motion", "human", "vehicle", "face"],

  "schedule": {
    "_note": "按通道×时段×类型的周计划；此处给默认模板，细粒度网格由 UI 生成写回",
    "template": "24x7_continuous",
    "event_always_on": true
  },

  "per_channel_override": [
    { "channel": 16, "mode": "event", "_note": "数字通道-前门：仅事件录，省空间" }
  ]
}
