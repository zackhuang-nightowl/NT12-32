# 待完成功能（填写表）

> 对照 2026-08-17 实码。核心出图 / 双轨录像 / 本机回放 / 8089 / TUTK live / 绑定握手 已通，不列入待接线。
> **填写**：做？填 `做` / `缓` / `不做`。优先级填 `P0` `P1` `P2`。备注自己写。

---

## 一、真机 / 板级（结构已在）

### 1. media_hal 内存池 / ddr_id

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 1 |  |

- **实现**：按板级 dts/`vendor_common_get_ddrid` 给 `hd_videodec` / `videoproc` 指定正确 DDR 池，不再写死 `ddr_id`。
- **优化**：避免解 16 路时分到错误内存池 → 花屏、解不动、偶发 OOM；把 VPU/显示带宽用满、少抢同一条 DDR。
- **现状**：2026-08-16 已做：所有解码路径（含非 8K）按 dts 填 DEC_IN/OUT/RATIO 的 `ddr_id`。

### 2. HDMI 4K 输出时序

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 1 |  |

- **实现**：`mhal_vout_init` 按分辨率配 `hd_videoout` 的 DEVCONFIG/OUTPUT（1080p / 4K30 / CVBS），屏幕不支持则沿现有阶梯降级。
- **优化**：真机 HDMI 能稳定点亮、切 4K 不黑屏；和 `setSysDisplay` 热切对齐，GUI 重启后画布不越界。
- **现状**：已有 `HD_VIDEOOUT_PARAM_MODE`：4K30 / 1080p60 / 720p60，失败沿阶梯降级。CVBS 未做。

### 3. OSD 真实叠加 — 不做（GUI 画）

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 否 | — | 固件不叠 HDMI OSD |

- **约定**：通道名 / 时间 / 事件图标由 LVGL 根据 `GUI_longPolling`（`ChannelStatusNotify` / Motion/Human/Face/Car / `RecordStatus`）自绘。
- **固件**：已移除 `mhal_vout_osd` 与 preview 文本合成。

### 4. YUV 抓拍拷贝

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 5 | 暂时不需要实现 记录下来 |

- **实现**：从硬解输出 mmap YUV，拷出一帧给 `rsdk_pic` / 事件快照 / App 要图。
- **优化**：抓拍不再依赖相机 JPEG 或空钩子；NVR 本机可对任意可见窗截当前画面，事件联动有图可传。
- **现状**：`mhal_vdec_recv` 标注 TODO，未做 phy→用户态拷贝。

### 5. BLE BlueZ GATT 0xFFF0

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 5 | 暂时不需要实现 记录下来 |

- **实现**：板级注册 GATT Service `0xFFF0`（RX `0xFFF1` write → `nvr_ble_on_rx`，TX `0xFFF2` notify），把手机 BLE 组包接到现有 8089 命令路由。
- **优化**：出厂无网时也能用 App/向导发现、配网、绑 owner；不必先插网线才能进 8089。
- **现状**：分/组包 + `ble_dispatch_bridge` 已接；`nvr_app.c` 里板级 link 是 TODO。

### 6. 32 路并录 + ≤16 窗 VPU/DDR 核验

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 暂时不需要实现 记录下来 | 5 | 解码有一套逻辑，暂时搁置 不实现。 |

- **实现**：真机压 32 路主+子双写、最多 16 窗硬解，按 `mhal_budget`（746 Mpix/s）做准入，超预算只录不显。
- **优化**：避免「设计能 32 路、板上解崩」；解码门控从估算变成可发布的上限表（哪路 4K 必须关窗）。
- **现状**：双拉双写、显示门控已在；VPU/DDR 上限未上机量。

### 7. 回放音频真机出声

| 做？ | 优先级 | 备 |
|------|--------|------|
| 需要实现 | 1 | 实现hdml的音频输出 |

- **实现**：本机回放 1X 正放时，盘上 AAC（主流 `stream=2`）经 `hd_audiodec`→`hd_audioout` 出喇叭/HDMI。
- **优化**：回放不再只有画面；`GUI_setPlaybackAudio` 从「写 enable[]」变成真的能听见。
- **现状**：2026-08-16 默认 `out_dev=HDMI(2)`；`MHAL_AOUT_DEV=0` 可改喇叭。真机出声待核。

### 8. 云存 TS PCR / 连续性

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 暂时不需要实现 记录下来 | 3 | 录像还需要调整 |

- **实现**：事件切片 MPEG-TS 补齐 PCR、continuity counter、音视频 PES 对齐，再 POST 到 VSaaS。
- **优化**：云端播不卡、不花、不丢同步；减少 VSaaS 拒片 / App 云回放跳帧。
- **现状**：`ts_mux.c` 极简 PAT/PMT/PES，上传链路已通。

### 9. 盘 SMART 明细

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 1 | 需要实现 |

- **实现**：`getAllDisksHealth` 读 SMART（温度、重映射扇区、通电时长、健康判定），不再恒「健康」。
- **优化**：满盘/坏盘能提前告警，避免静默写坏；GUI 存储页有真实健康而不是桩数据。
- **现状**：2026-08-16 `nvr_storage_read_health` 已接 `rsdk_smart`（ATA SMART 温度/重映射/待映射/通电时长）。`getAllDisksHealth` 走同一套。

### 10. PoE 即插即用鉴权握手

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已实现 | 1 | 2026-08-17 落地 |

- **实现**：PoE 口扫到相机后，按设备三类走激活/增强安全握手，再取流绑定。
- **优化**：插上就能出图，不再因空密码/未激活卡在 `onvif_auto`；减少对局域网别人相机的误绑（LAN 仍只显式添加）。
- **现状**：2026-08-17 已落地。发现口=命令口；三类都走 ONVIF 取流；首次添加试一轮（用户密码→P_enh→P_act→无鉴权），失败 status 4 等 `setLanDevice`。一机一 `nop_onvif_device_t`。

---

## 二、半成品 / 桩

### 11. TUTK 远程回放 RTSP

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已实现 | 1 | 2026-08-19 隧道 RTSP 接线；真机对 App 回归 |

- **实现**：App `startPlayback` 拿到隧道 RTSP URL；NVR 用 `rsdk_group_play` 取帧，打 RTP（含拓展头）经 P2PTunnel 给手机（与 live 共用 `nvr_rtsp_live` :8554）。
- **优化**：远程不只看实时；出差/云端可拖时间轴回放，不必站在 HDMI 前。
- **现状**：`startPlayback` / Seek `SET_PARAMETER` / 空白帧已接线。待真机对 App Timeline 回归。

### 12. DG 私有密码（增强模式 / 激活 AES256）

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已实现 | — | 密钥已写入固件 |

- **实现**：接入 NightOwl-DG 算法：`random→password`、激活口令 AES256；`nvr_pw_algo_ready()` 置 1。
- **优化**：相机增强安全/激活不再走明文或空 key；PoE 即插即用（#10）才能对外发布。
- **现状**：2026-08-17 增强 `eT79Uo51sK` / `SzxGJDZxjJ`；激活 AES-**256-ECB**（无 IV），key=ASCII `eT79Uo51sK` 补 0 到 32 字节。`nvr_pw_act_encrypt` 走 ECB。设置库 `factory.keyx/keyy` 仍可覆盖。

### 13. 电子放大 ZoomPan

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 1 |  |

- **实现**：`X_NightOwl_setChannelZoomPan` 驱动 `hd_videoproc` ROI 裁剪（单画面放大/平移），get 回真实生效值。
- **优化**：GUI 电子放大从「滑条能动、画面不变」变成真放大；和已有 `mhal` crop 路径对齐。
- **现状**：8089 `X_NightOwl_setChannelZoomPan` 已接到 `mhal_vout_set_crop`。2026-08-17 修：已 start 的 VPE 先 stop 再 set IN_CROP 再 start（原先只 set + 对已启动路径 start，画面不变）；解码器后开时补下暂存 ROI。

### 14. 推送通知外发

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 2 | 等1的实现才开始 |

- **实现**：通道推送开关 + 免打扰生效后，AI/移动事件经 HTTPS 推到 NightOwl 推送服务（手机出横幅）。
- **优化**：人不在 HDMI 前也能收到人形/门铃；开关不再是「只写库、永远不发」。
- **现状**：`set/getChannelsPushNotificationSwitch` 已落 `push_config`；外发未做。

### 15. 邮件 SMTP SSL/465

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 5 |  |

- **实现**：`GUI_testEmailAlert` / 告警发送支持 SMTPS 465（OpenSSL），与现有 25/587 并列。
- **优化**：兼容只开 465 的企业邮箱；测试邮件不再因「暂未实现 SSL」失败。
- **现状**：配置读写在；`nvr_netime.c` 明确 465 未做。

### 16. NetPort 热切换

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 不需要 | 5 | 暂时不提供功能，回复501 |

- **实现**：`GUI_setNetPort` 改 8089/554 后重启 `nop_http_server` 与 `nvr_rtsp_live`，TUTK 白名单同步新端口。
- **优化**：改端口不用整机重启；避免「库里是新口、进程仍听旧口」导致 GUI/App 连不上。
- **现状**：2026-08-16 `GUI_setNetPort` 回 501 NOT_SUPPORT。`getNetPort` 仍可读。

### 17. 事件 → 抓拍联动

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已做 | 1 | 缩略图仅隧道 GET |

- **实现**：8012 JPEG 或 ONVIF GetSnapshot → 独立线程 `rsdk_pic`；`queryEventList.thumbnailUrl` = `http://iotc-tunnel:8089/eventSnap?eid=`（有图才填）。GET 挂 8089，POST JSON 不变；App 只经 TUTK 映射。
- **优化**：事件列表有图可点。
- **现状**：2026-08-17 已串。

### 18. OTA 校验 + 真烧写

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已做 | 1      | 真机 nandwrite 待核 |

- **实现**：下载/本地 .rom 后校验 **MD5**；包版本必须 **高于** `system.fw_version`，除非强制升级；通过后 `nvr_do_update.sh` 写非活动 A/B rootfs 并切槽重启。
- **优化**：升级可发布：防下错包、防半包变砖；GUI 进度可信。
- **现状**：2026-08-17 `nvr_ota.c`：payload MD5 必校；包版本必须更高，除非强制（头 `force=1` / 命令 `force` / `nopUpgrade` 无 version）。更新器刷对面槽（mtd6/mtd7），当前槽留作备份。
  2026-08-19：`GUI_checkServerFirmware` 查 NightOwl OTA；IPC 由 NVR 下载后 NOP `upload.cgi` / ONVIF `StartFirmwareUpgrade` 下推。

---

## 三、未开始（里程碑）

### 19. Wizard 四场景

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 需要实现 | 2 | 1实现后才开始 |

- **实现**：首次上电向导：出厂发现 → 设管理员 → 绑 NOP 账户 → 加相机/时区/盘，四条路径走完进预览。
- **优化**：产线/用户不用先会 8089 命令；和 BLE（#5）、远程门控、setOwner 串成一条体验。
- **现状**：底层命令大多已有；没有向导状态机。

### 20. LVGL GUI 本体

| 做？ | 优先级 | 备注 |
|------|--------|------|
|  |  |  |

- **实现**：独立 LVGL 进程（预览/回放/菜单/登录），经 `127.0.0.1:8089` 驱本固件；本仓只保证协议。
- **优化**：HDMI 上有可点的产品界面，而不是只有后端。
- **现状**：看护脚本会拉 GUI 二进制；界面在独立仓/独立二进制。

### 21. 双向对讲

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已做 | 2 | HDMI/RCA MIC 采集未接 |

- **实现**：`startSpeaker`/`stopSpeaker` 本地处理。本机听 `127.0.0.1:7000`（TUTK 映射 `iotc-tunnel:7000`）。NOP → 相机 TCP 7000；ONVIF → RTSP backchannel。相机 MIC→App 仍走已有拉流音频。
- **优化**：门铃/对讲能对相机说话。
- **现状**：2026-08-17 已串；`preferProtocol=rca` 只武装相机侧，HDMI 采集后续。

### 22. 34569 LocalLAN / MAC 找回

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已做 | 2 | 2026-08-17 |

- **实现**：局域网 34569 发现相机（IP/MAC/机型）并走同一绑定；已添加 LAN 掉线按 MAC 找回当前 IP；本机听 34569 供 App 找回 NVR。
- **优化**：路由器改 DHCP 后仍能找到相机与 NVR，不必串口或扫整网。
- **现状**：`nvr_lan34569.c` 已接线：`GUI_LanSearch`、LAN tick 召回、`nvr_app` 本机应答。

### 23. 无盘 BaseStation 实时上传

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 暂不需要 |  |  |

- **实现**：无 HDD 时拉流直接 tee 到云存（不落裸盘），作 BaseStation 形态。
- **优化**：低成本无盘 SKU 也能云回放；和现有「有盘切片上传」互补。
- **现状**：明确 v2；tee 点在 streaming 旁路已标注。

### 24. Chromecast

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 暂不需要 |  |  |

- **实现**：把预览/回放投到 Google Cast 设备。
- **优化**：客厅大屏不经过本机 HDMI 线。
- **现状**：未开始。

### 25. 产测 ProductionTest

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 暂不需要 |  |  |

- **实现**：产线命令：网口/PoE/HDMI/HDD/按键/LED/MAC/SN 自检，出 PASS/FAIL。
- **优化**：工厂不用靠人工点 GUI；烧录后一键判良品。
- **现状**：未开始。

### 26. 周维护自动重启

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 已做 | 1 |  |

- **实现**：按 `GUI_setAutoRebootSetting` 的星期/时刻，在空闲窗口重启，清泄漏/重挂盘。
- **优化**：长期运行稳定性；设置项从「能读写」变成「到点真重启」。
- **现状**：2026-08-16 主循环每 60s 评估；`WhichDay` 0=每天/1-7=周一..周日；`WhichTime` 0-23 整点或 HHMM；同一日历日只重启一次。

### 27. 电池机 / 无线相机配对

| 做？ | 优先级 | 备注 |
|------|--------|------|
| 暂不需要 |  |  |

- **实现**：`startAddWirelessCameras` 等：无线/电池机配对、低功耗唤醒。
- **优化**：产品线从「有线 PoE+LAN」扩到电池机。
- **现状**：本期默认不做（STATUS 已标忽略）。

---

## 你圈定后的下一刀（选填）

先做哪几号：

不做、从本表划掉：

补充（表里没有的）：
