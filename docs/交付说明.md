# NT12-32 NVR 固件 — 交付说明

> SoC：Novatek NT98633 / NA51090（4×Cortex-A53，aarch64，Linux 4.19）。规格：32 路 / 16 PoE。
> 本次交付：整机固件 `nvr_app`（aarch64）+ 界面进程 + 看护脚本 + 部署包。蓝牙(BLE)按要求未纳入。

---

## 1. 交付物清单

| 交付物 | 路径 | 说明 |
|---|---|---|
| 固件二进制 | `build_arm/app/nvr_app` | ELF aarch64，~9 MB（未 strip，带符号） |
| 部署包 | `deploy/rootfs/` | 按设备路径摆好，合并进根文件系统即可 |
| 界面进程 | `deploy/rootfs/dvr/bin/NO_xVR_GUI_V2-ca53-2.1.3_argb1555` | 你提供的 LVGL 界面（aarch64） |
| 看护脚本 | `deploy/rootfs/dvr/bin/nvr_supervisor.sh` | 起 NVR→等8089→起界面→崩溃重启 |
| 开机自启 | `deploy/rootfs/etc/init.d/S99nvr` | busybox init.d |
| NVR 配置 | `deploy/rootfs/dvr/config/*.json` | 首启种子(生成 nvr_settings.db/meta.db) |
| 界面配置 | `deploy/rootfs/mnt/custom/*` | GUI_CONFIG.json / gui_cfg.ini / 字体等 |

---

## 2. 功能实现对照（按 videoRecorder 文档）

口径：**✅ 已实现并接线**（本二进制在跑）· **🟡 结构/handler 在，未完全接线 / 需真机或 DG 算法** · **❌ 未实现**。

### 2.1 AddCamera 相机接入
| 能力 | 状态 | 代码 / 说明 |
|---|---|---|
| ONVIF 发现三分类(NOP/nopOnvif/onvif) | ✅ | `nvr_dev_classify`(16 自测) |
| 取流 URL(ONVIF GetStreamUri) | ✅ | `nvr_onvif_get_url` |
| 通道在线状态机 + 断线退避重连 | ✅ | `app/channel/nvr_channel.c` |
| PoE 口→通道 即插即用绑定 | 🟡 | `nvr_chan_bind_poe` 在；发现循环+VLAN/DHCP 组网(系统级)未接 |
| 3 种连接出图(增强/激活/通用) | 🟡 | 编排在；密码握手待 NightOwl-DG 算法(`nvr_pw_*` 占位) |
| LAN Add 命令集 | 🟡 | nop handler 在，未接通道管理 |

### 2.2 GUI 本地界面（预览/回放/菜单/鉴权）
| 能力 | 状态 | 说明 |
|---|---|---|
| 界面↔NVR(8089 /APPJsonCmd) | ✅ | 固件起 NOP 8089 后端 |
| 实时预览 1/4/6/9/12/16 分屏 + OSD | ✅ | `app/preview/nvr_preview.c`(mhal_vout) |
| 拉流出图(RTSP→硬解→上屏) | ✅ | `streaming` + `media_hal`(hdal) |
| 回放(检索/跨盘/导出MP4) | ✅ | recorder API |
| Admin 鉴权/锁定/找回 | 🟡 | 设置库 `auth` 表在；GUI_login handler 在 |

### 2.3 Storage 存储
| 能力 | 状态 | 说明 |
|---|---|---|
| 录像引擎(裸盘/环形/索引/AES/多盘/导出/元数据/抓拍) | ✅ | `recorder` librsdk(8 example PASS) |
| 盘管理(发现/格式化/装配/健康/热插拔) | ✅ | `storage`(SMART 明细待接平台) |
| getStorageInfo/format/health(NOP) | 🟡 | cap_storage 接真实盘状态需接线 |

### 2.4 cloudRec 云存
| 能力 | 状态 | 说明 |
|---|---|---|
| 事件切片上传引擎(取段→TS→VSaaS→回写状态) | ✅ | `cloud_uploader`，按设置门控启动 |
| 录像云存状态内置 recorder | ✅ | `rsdk_cloud`(20 自测) |
| 多通道 starttime 埋通道 + -1002/3/4 强制关 | ✅ | `uploader.c` |
| setOwner/stoken 持久化 + 开关/配置 | 🟡 | 设置库表在；cap_cloud/cap_misc 仍静态量，待接 |
| 同步(无盘)实时上传 | ❌ | v2 延后 |

### 2.5 RemoteClients / Admin_NOP / pushNotification
| 能力 | 状态 | 说明 |
|---|---|---|
| TUTK P2P(登录/监听/推流) | 🟡 | glue 在；未在 nvr_app 启动(链接注释) |
| owner/账户/stoken | 🟡 | 设置库表 + nop handler 在，未接线 |
| 事件→推送 | 🟡 | 事件中枢 publish→nop push 引擎(结构在) |

### 2.6 其余
| 域 | 状态 |
|---|---|
| NOP↔ONVIF 映射 9 域(PTZ/OSD/隐私/媒体/AI/事件…) | ✅ |
| 三档路由(非透传/透传/翻译) | 🟡 活文档 `NOP_NONPASSTHROUGH_APIS.md` |
| OTA(自升级/IPC下推) | ❌ handler 在，流程未实现 |
| talk 双向对讲 | ❌ |
| Wizard 向导 | 🟡 handler 在，UI 由界面驱动 |
| BLE | ❌ 本期不做 |
| Chromecast / ProductionTest / BaseStation | ❌ |

---

## 3. 组件与自测

| 组件库 | 作用 | 自测 |
|---|---|---|
| `nvr_settings` | SQLite 可写配置库 | 24 ✅ |
| `nvr_crypto` | MD5/SHA/AES256 | 10 ✅(FIPS 向量) |
| `rsdk`(recorder) | 录像引擎 + `rsdk_cloud` | 8 example + 20(cloud) ✅ |
| `nvr_dev_classify` | 设备三分类 | 16 ✅ |
| `nvr_app_core` | app 集成层(channel/preview/record_sched/event) | 主机编译校验 ✅ |
| `nvr_cloud_uploader` | 云存上传引擎 | 编译 ✅ |
| `nvr_streaming`/`nvr_storage`/`nvr_onvif`/`cloud_tutk`/`nopcore` | 拉流/存储/onvif/P2P/协议 | 编译 ✅ |
| `sqlite3`/`cjson`/`happytime_rtsp`/`mhal` | vendored/平台 | 交叉编译 ✅ |

主机 `ctest` 4/4 通过；交叉构建自动关主机自测/示例。

---

## 4. 构建

| 目标 | 命令 |
|---|---|
| 主机(库+自测) | `cmake -S . -B build && cmake --build build -j && ctest --test-dir build` |
| 交叉(整机固件) | `cmake -S . -B build_arm -DCMAKE_TOOLCHAIN_FILE=cmake/toolchain-aarch64-ca53.cmake -DNVR_WITH_ONBOARD=ON -DBSP_ROOT=/home/zack/NT12-32/NT98633/na51090_linux_sdk && cmake --build build_arm -j` |

先决条件：主机 `libsqlite3-dev libssl-dev libcurl4-openssl-dev python3`（其中 sqlite3 已 vendored，可选）；交叉需 na51090 BSP + `aarch64-ca53` 工具链。详见 `BUILD.md`。

---

## 5. 部署布局（设备路径）

| 设备路径 | 内容 |
|---|---|
| `/dvr/bin/nvr_app` | 固件 |
| `/dvr/bin/NO_xVR_GUI_...argb1555` | 界面 |
| `/dvr/bin/nvr_supervisor.sh` | 看护脚本 |
| `/dvr/config/*.json` | NVR 配置(首启种子) |
| `/mnt/custom/*` | 界面配置(字体/GUI_CONFIG/…) |
| `/etc/init.d/S99nvr` | 开机自启 |

启动：`/etc/init.d/S99nvr start`（或直接 `/dvr/bin/nvr_supervisor.sh`）。

---

## 6. 运行期依赖（设备 rootfs 需具备，原厂固件均带）

| 库 | 来源 |
|---|---|
| `libssl.so.3` `libcrypto.so.3` | OpenSSL 3.x(设备/ TUTK) |
| `libcurl.so.4` | 设备/ TUTK |
| `libhdal.so` | na51090 BSP |
| `libpthread/libdl/libm/libstdc++/libgcc_s/libc` | 标准运行库 |

---

## 7. 日志 / 排障

| 项 | 说明 |
|---|---|
| 日志设施 | `common/include/nvr_log.h`，格式 `[时:分:秒] 级别/模块: 消息` |
| 级别控制 | 环境变量 `NVR_LOG_LEVEL`(0=E..3=D，默认 2=I)，运行期可调 |
| 打点 | chan(绑定/上线/掉线/重连/PoE/分类) · event(AI事件) · rec(事件录像) · cloud(队列/上传/拒绝/完成) |
| 串口 | 内核 `console=ttyS0,115200`；看护默认 `LOG=/dev/console`=ttyS0，**日志上串口(COM29)** |

---

## 8. 待接线清单（建议优先级）

| 优先级 | 项 | 接完效果 |
|---|---|---|
| 高 | TUTK P2P 启动接进 nvr_app | 远程 App 可连 |
| 高 | NOP cap_cloud/cap_misc → 设置库 | 云存开关/owner/stoken 端到端持久化 |
| 高 | PoE 自动发现绑定循环 + eth1 VLAN/DHCP | 16 路即插即用真正自动上图 |
| 中 | 3 方案连接密码握手(需 NightOwl-DG 算法) | nopOnvif/增强设备自动出图 |
| 中 | Admin 鉴权/锁定、LAN Add 接通道 | 本地登录 + 数字通道添加 |
| 低 | OTA / 对讲 / 推送落地 | 产品外围 |
