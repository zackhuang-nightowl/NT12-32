# 出厂烧录分区数据(TODO:待补充实现)

> 记录:2026-08-06 · 状态:**待实现**(占位,后面补)

## 背景

以下每台设备唯一的出厂数据,**不进编译期默认(nvr_defaults.h)**,由产测**烧录到独立分区**,运行期读取:

| 数据 | 说明 | 当前状态 |
|---|---|---|
| `sn` | 设备序列号 | 现读 `system.sn`(设置库),默认空;应改为从烧录分区读 |
| `mac` | MAC 地址 | 现读 `system.mac`,默认空 |
| `NID` | 设备网络 ID | 未接 |
| `keyx` | 密钥 X(增强/激活口令用,见 [nvr_crypto.c] `NVR_ENH_KEY_X`) | 现为空占位桩 |
| `keyy` | 密钥 Y(同上 `NVR_ENH_KEY_Y`) | 现为空占位桩 |

## 待实现

1. 确定烧录分区(设备名/挂载点/格式,如 `/mnt/factory` 或 mtd 分区)。
2. 写一个读取模块(如 `nvr_factory.c`):开机读 sn/mac/NID/keyx/keyy → 填入运行期(设置库或内存)。
3. `keyx/keyy` 接到 crypto:点亮增强口令 / AES 激活(现 `nvr_pw_from_random`/`nvr_pw_act_encrypt` 因空 key 未启用,见 [死代码清理清单.md](死代码清理清单.md) 链路 9)。
4. `getDeviceInfo` 的 sn 改为从分区读(现默认空)。

> 分区路径/格式确定后,补进本文档并实现。
